// --- МЕТАДАННЫЕ ДОКУМЕНТА ---
#let project-title = "Доказательства СРВМ"
#let project-version = "ν. 11/06 04:20"
#let project-course = "ITMO SE y29. Rzhonsnicskaya J.B. & Gonchar L.I. & Brylevskaya L. I. course."
#let project-motto = "Every person with higher education must go through a BEE"

// --- НАСТРОЙКИ СТИЛЯ И СТРАНИЦЫ ---
#set page(
  paper: "a4",
  margin: (top: 2.5cm, bottom: 2.5cm, left: 3cm, right: 1.5cm),
  numbering: "1",
  number-align: center,
  // Хедер для всех страниц, кроме первой (титульной)
  header: context {
    if counter(page).get().first() > 1 {
      align(right, text(fill: gray, size: 9pt, style: "italic")[#project-title])
    }
  }
)

// Шрифты и выравнивание текста
#set text(font: "Liberation Serif", lang: "ru", size: 11pt)
#set par(justify: true, leading: 0.65em)
#set heading(numbering: none)

// Стилизация заголовков
#show heading: it => block(below: 0.8em)[
  #v(0.5em)
  #text(weight: "bold", fill: rgb("1a1a1a"))[#it.body]
]

// --- ВСПОМОГАТЕЛЬНЫЕ ФУНКЦИИ (ШАБЛОНЫ БЛОКОВ) ---

// Блок "Суть на пальцах" с аккуратной левой границей
#let intuitively(body) = block(
  fill: rgb("f8f9fa"),
  stroke: (left: 3pt + rgb("4a90e2")),
  inset: (x: 1.2em, y: 1em),
  radius: (right: 4pt),
  width: 100%,
  above: 1em,
  below: 1em,
  [
    #text(weight: "bold", fill: rgb("4a90e2"))[Суть на пальцах:] \
    #v(0.3em)
    #text(style: "italic", size: 10.5pt)[#body]
  ]
)

// Блок для оформления конкретного вопроса/билета
#let question(num, title, requirements, body) = {
  block(width: 100%, breakable: false)[
    #heading(level: 1)[Вопрос #num. #title]
    #if requirements != none [
      #block(
        fill: rgb("fff3cd"),
        stroke: 0.5pt + rgb("ffeeba"),
        inset: 0.8em,
        radius: 4pt,
        width: 100%,
        text(fill: rgb("856404"), size: 10pt)[#requirements]
      )
    ]
  ]
  v(0.5em)
  body
}

// Блок для бонус-примеров с правильными отступами
#let bonus(body) = block(
  fill: rgb("e2f0d9"),
  stroke: (left: 3pt + rgb("70ad47")),
  inset: (x: 1.2em, y: 1em),
  radius: (right: 4pt),
  width: 100%,
  above: 1em,
  below: 1em,
  [
    #text(weight: "bold", fill: rgb("385723"))[Бонус-пример:] \
    #v(0.3em)
    #body
  ]
)


// --- ТИТУЛЬНАЯ СТРАНИЦА ---
#page(numbering: none)[
  #align(center + horizon)[
    // Главный заголовок
    #text(size: 26pt, weight: "bold", fill: rgb("2c3e50"))[#project-title]
    
    #v(0.5cm)
    // Версия / Дата счета
    #text(size: 11pt, font: "Liberation Mono", fill: gray)[#project-version]
    
    #v(1.5cm)
    #image("j8ske.png", width: 65%)
    
    #v(1.5cm)
    // Данные курса
    #text(size: 12pt, weight: "medium")[#project-course]
    
    #v(2cm)
    // Фирменная цитата-мотто в рамке
    #block(
      width: 85%, 
      stroke: (top: 0.5pt + gray, bottom: 0.5pt + gray), 
      inset: (y: 1em)
    )[
      #text(size: 11pt, style: "italic", fill: rgb("555555"))["#project-motto"]
    ]
  ]
]

// --- ОГЛАВЛЕНИЕ ---
#pagebreak()
#text(weight: "bold")[#outline(title: "Оглавление", depth: 2)]
#pagebreak()

#question(
  1, 
  "Неравенство Коши-Буняковского-Шварца",
  [
    *Требования билета:*
    + Сформулируйте и докажите неравенство КБШ для вещественного евклидова пространства.
    + Обоснуйте критерий обращения неравенства в точное равенство.
    + Разберите тривиальный и общий случаи.
  ],
  [
    #intuitively[
      Представь себе физическую работу силы. Самый эффективный способ двигать тяжелый шкаф — толкать его ровно в том направлении, куда он перемещается. Если ты толкаешь шкаф строго вперед, вся твоя энергия (100%) уходит в полезную работу. Если же ты начнешь толкать его под углом, часть твоих усилий будет тратиться впустую на вдавливание шкафа в стену.

      Неравенство Коши-Буняковского-Шварца (КБШ) — это математическое отражение этого принципа "коэффициента полезного действия". Скалярное произведение векторов — это аналог совершенной работы, а произведение их длин (норм) — это общая затраченная энергия. Неравенство утверждает, что "полезная работа" никогда не превысит полную энергию. С геометрической точки зрения это эквивалентно тому, что косинус угла между векторами по модулю не может быть больше единицы ($|cos theta| <= 1$).

      Максимальная эффективность (точное равенство в КБШ) достигается тогда и только тогда, когда шкаф толкают идеально по направлению движения — то есть когда векторы сонаправлены (или противоположно направлены). В линейной алгебре это состояние идеального совпадения направлений называется линейной зависимостью. Если векторы смотрят в разные стороны и не лежат на одной прямой, часть "энергии" неминуемо теряется, превращаясь в строгое неравенство.
    ]

    === Строгая формулировка

    Пусть $E$ — вещественное евклидово пространство со скалярным произведением $⟨·, ·⟩$ и индуцированной нормой $||x|| = sqrt(⟨x, x⟩)$.

    *Теорема (Неравенство Коши-Буняковского-Шварца):* \
    Для любых векторов $x, y in E$ справедливо неравенство:
    $ ⟨x, y⟩^2 <= ⟨x, x⟩ dot ⟨y, y⟩ $

    Или в терминах норм векторов:
    $ |⟨x, y⟩| <= ||x|| dot ||y|| $

    *Критерий обращения в равенство:* \
    Равенство $⟨x, y⟩^2 = ⟨x, x⟩ dot ⟨y, y⟩$ достигается тогда и только тогда, когда система векторов $\{x, y\}$ является линейно зависимой.

    === Пошаговое аналитическое доказательство

    Разбиваем доказательство на два случая в зависимости от тривиальности векторов.

    ==== Шаг 1. Анализ тривиального случая
    Пусть один из векторов является нулевым. Без потери общности положим $y = 0$.
    + По свойствам скалярного произведения, скалярное произведение любого вектора с нулевым вектором равно нулю: $⟨x, 0⟩ = 0$.
    + Норма нулевого вектора также равна нулю: $||0|| = 0$.
    
    Подставляя эти значения в неравенство, получаем тождество: 
    $0^2 <= ||x||^2 dot 0 => 0 <= 0$
    
    В данном случае достигается точное равенство. Система векторов $\{x, 0\}$ линейно зависима, так как существует нетривиальная линейная комбинация $0 · x + 1 · 0 = 0$, где коэффициент перед нулевым вектором не равен нулю. Таким образом, для тривиального случая теорема и критерий верны.

    ==== Шаг 2. Анализ общего случая (Ненулевые векторы)
    Пусть теперь оба вектора отличны от нуля: $x != 0$ и $y != 0$. Из этого следует, что $⟨y, y⟩ > 0$ в силу аксиомы положительной определенности скалярного произведения.

    Рассмотрим вспомогательный вектор $z(t) = x + t y$, где $t in bb(R)$ — произвольный вещественный параметр. По той же аксиоме положительной определенности, скалярный квадрат любого вектора всегда неотрицателен:
    $ ⟨z(t), z(t)⟩ >= 0 quad forall t in bb(R) $

    Распишем это выражение, используя аксиомы линейности и симметрии скалярного произведения:
    $ ⟨x + t y, x + t y⟩ = ⟨x, x⟩ + ⟨x, t y⟩ + ⟨t y, x⟩ + ⟨t y, t y⟩ >= 0 $

    Вынося вещественный параметр $t$ за знак скалярного произведения, получаем:
    $ ⟨x, x⟩ + t ⟨x, y⟩ + t ⟨y, x⟩ + t^2 ⟨y, y⟩ >= 0 $

    Так как в вещественном евклидовом пространстве выполняется свойство симметрии $⟨x, y⟩ = ⟨y, x⟩$, мы можем объединить средние слагаемые в квадратный трехчлен:
    $ ⟨y, y⟩ t^2 + 2 ⟨x, y⟩ t + ⟨x, x⟩ >= 0 $

    ==== Шаг 3. Применение критерия знака квадратного трехчлена
    Полученное выражение представляет собой квадратный трехчлен относительно переменной $t$ вида $f(t) = A t^2 + B t + C$, где:
    - $A = ⟨y, y⟩ = ||y||^2 > 0$
    - $B = 2 ⟨x, y⟩$
    - $C = ⟨x, x⟩ = ||x||^2$

    Графиком функции $f(t)$ является парабола, ветви которой направлены вверх ($A > 0$). Квадратный трехчлен неотрицателен для всех вещественных $t$ тогда и только тогда, когда парабола лежит не ниже оси абсцисс. 
    
    Это означает, что уравнение $f(t) = 0$ имеет не более одного вещественного корня, что эквивалентно условию неотрицательности четверти дискриминанта ($D/4 <= 0$):
    $ D/4 = (B/2)^2 - A C = ⟨x, y⟩^2 - ⟨y, y⟩ dot ⟨x, x⟩ $

    Требуем выполнения условия $D/4 <= 0$:
    $ ⟨x, y⟩^2 - ⟨y, y⟩ dot ⟨x, x⟩ <= 0 => ⟨x, y⟩^2 <= ⟨x, x⟩ dot ⟨y, y⟩ $

    Извлекая квадратный корень из обеих частей неравенства, получаем финальный вид КБШ:
    $ |⟨x, y⟩| <= ||x|| dot ||y|| $

    ==== Шаг 4. Доказательство критерия обращения в равенство
    Докажем критерий в обе стороны.

    *Необходимость ($=>$):* \
    Пусть выполняется точное равенство: $⟨x, y⟩^2 = ⟨x, x⟩ dot ⟨y, y⟩$. Это означает, что четверть дискриминанта строго равна нулю: $D/4 = 0$. 
    
    Если дискриминант квадратного трехчлена равен нулю, то существует единственный вещественный корень $t_0$, при котором значение трехчлена зануляется:
    $ f(t_0) = ⟨x + t_0 y, x + t_0 y⟩ = 0 $

    В силу аксиомы положительной определенности скалярного произведения, равенство скалярного квадрата вектора нулю означает, что сам вектор равен нулю:
    $ x + t_0 y = 0 => x = -t_0 y $
    Мы получили, что вектор $x$ линейно выражается через вектор $y$. Следовательно, векторы $x$ и $y$ линейно зависимы.

    *Достаточность ($arrow.l.double$):* \
    Пусть векторы $x$ и $y$ линейно зависимы. Если $y = 0$, то равенство доказано на Шаге 1. Если $y != 0$, то из линейной зависимости следует, что вектор $x$ можно представить в виде $x = alpha y$ для некоторого вещественного числа $alpha in bb(R)$. 
    
    Подставим это выражение в обе части исходного неравенства КБШ:
    - *Левая часть:* $⟨x, y⟩^2 = ⟨alpha y, y⟩^2 = (alpha ⟨y, y⟩)^2 = alpha^2 ⟨y, y⟩^2$
    - *Правая часть:* $⟨x, x⟩ dot ⟨y, y⟩ = ⟨alpha y, alpha y⟩ dot ⟨y, y⟩ = alpha^2 ⟨y, y⟩ dot ⟨y, y⟩ = alpha^2 ⟨y, y⟩^2$

    Левая часть тождественно равна правой. Критерий полностью доказан.

    #bonus[
      Рассмотрим пространство $bb(R)^3$ со стандартным скалярным произведением $⟨a, b⟩ = a_1 b_1 + a_2 b_2 + a_3 b_3$.

      *Пример 1. Проверка строгого неравенства (Линейно независимые векторы)* \
      Возьмем два линейно независимых вектора: $x = (1, 2, 3)$ и $z = (1, 0, -1)$.
      - Скалярное произведение: $⟨x, z⟩ = 1 · 1 + 2 · 0 + 3 · (-1) = 1 + 0 - 3 = -2$
      - Квадрат скалярного произведения: $⟨x, z⟩^2 = (-2)^2 = 4$
      - Скалярные квадраты векторов: 
        $⟨x, x⟩ = 1^2 + 2^2 + 3^2 = 14$ \
        $⟨z, z⟩ = 1^2 + 0^2 + (-1)^2 = 2$
      
      Проверяем КБШ: $4 <= 14 · 2 => 4 < 28$. Неравенство строгое, так как векторы линейно независимы.

      *Пример 2. Проверка критерия равенства (Линейно зависимые векторы)* \
      Возьмем вектор $y$, коллинеарный вектору $x$ (умножим $x$ на 2): $y = (2, 4, 6)$.
      - Скалярное произведение: $⟨x, y⟩ = 1 · 2 + 2 · 4 + 3 · 6 = 28$
      - Квадрат скалярного произведения: $⟨x, y⟩^2 = 28^2 = 784$
      - Скалярный квадрат вектора $y$: $⟨y, y⟩ = 2^2 + 4^2 + 6^2 = 56$
      
      Проверяем критерий равенства КБШ: $14 · 56 = 784$. Получаем точное равенство $784 = 784$, что подтверждает доказанный критерий.
    ]
  ]
)

#question(
  2, 
  "Комплексные евклидовы (унитарные) пространства",
  [
    *Требования билета:*
    - Дайте определение скалярного произведения в комплексном линейном пространстве. Приведите полную систему аксиом.
    - Докажите свойство полулинейности по второму аргументу: $⟨x, alpha y + beta z⟩ = overline(alpha)⟨x, y⟩ + overline(beta)⟨x, z⟩$, опираясь на аксиомы эрмитовой симметрии и линейности по первому аргументу.
    - Приведите пример вычисления скалярного произведения в $bb(C)^2$ для векторов с комплексными координатами.
  ],
  [
    #intuitively[
      В вещественных пространствах геометрия работает привычно: длина вектора возводится в квадрат как $x^2$, и это число всегда неотрицательно. Но представь, что мы попытаемся перенести стандартное скалярное произведение в комплексный мир "в лоб", просто перемножая координаты. Возьмем одномерный вектор $z = (i)$. Его "скалярный квадрат" по старой формуле равен $i dot i = -1$. Мы получили отрицательный квадрат длины! В геометрии это катастрофа: длина пространства не может быть мнимой или отрицательной, иначе мы не сможем измерять расстояния между точками.

      Чтобы спасти геометрию, математики придумали хитрость — "зеркальное сопряжение". При вычислении скалярного произведения координаты второго вектора обязательно переворачиваются в комплексном смысле (над ними ставится черта сопряжения $overline(z)$). Тогда "длина" нашего вектора превратится в $i dot overline(i) = i dot (-i) = -i^2 = 1$. Всё встало на свои места, длина снова вещественна и положительна!

      Однако за это спасение приходится платить асимметрией. Если в вещественном пространстве от переменны мест векторов ничего не менялось ($⟨x, y⟩ = ⟨y, x⟩$), то в комплексном пространстве зеркало сопряжения переворачивает результат при смене мест: $⟨x, y⟩ = overline(⟨y, x⟩)$. Это свойство называется эрмитовой симметрией. Именно из-за него, когда мы пытаемся вынести константу из второго (сопрягаемого) аргумента, она послушно "проходит сквозь зеркало" и тоже приобретает черту сопряжения. Это свойство называют полулинейностью (или косолинейностью).
    ]

    === Строгая формулировка и аксиоматика

    Пусть $V$ — линейное пространство над полем комплексных чисел $bb(C)$.

    *Определение:* Скалярным произведением в комплексном линейном пространстве $V$ называется функция, которая каждым двум векторам $x, y in V$ ставит в соответствие комплексное число $⟨x, y⟩ in bb(C)$, и удовлетворяет следующей системе из четырех аксиом:

    - *Эрмитова симметрия:* Для любых векторов $x, y in V$ справедливо соотношение:
      $ ⟨x, y⟩ = overline(⟨y, x⟩) $
      (где черта означает комплексное сопряжение).
    - *Аддитивность по первому аргументу:* Для любых векторов $x_1, x_2, y in V$:
      $ ⟨x_1 + x_2, y⟩ = ⟨x_1, y⟩ + ⟨x_2, y⟩ $
    - *Однородность по первому аргументу:* Для любого вектора $x, y in V$ и любого комплексного скаляра $alpha in bb(C)$:
      $ ⟨alpha x, y⟩ = alpha ⟨x, y⟩ $
    - *Положительная определенность:* Для любого вектора $x in V$ его скалярный квадрат является вещественным числом, причем:
      $ ⟨x, x⟩ >= 0, quad "и" quad ⟨x, x⟩ = 0 <=> x = 0. $

    Линейное пространство со скалярным произведением, удовлетворяющим этим аксиомам, называется *комплексным евклидовым (или унитарным) пространством*.

    *Теорема (о полулинейности по второму аргументу):* \
    В унитарном пространстве скалярное произведение обладает свойством полулинейности по второму аргументу, то есть для любых векторов $x, y, z in V$ и любых скаляров $alpha, beta in bb(C)$ выполняется:
    $ ⟨x, alpha y + beta z⟩ = overline(alpha)⟨x, y⟩ + overline(beta)⟨x, z⟩ $

    === Пошаговое аналитическое доказательство

    Докажем теорему о полулинейности. Нам нужно раскрыть выражение $⟨x, alpha y + beta z⟩$, используя только аксиомы эрмитовой симметрии, линейности по первому аргументу и базовые свойства комплексных чисел.

    ==== Шаг 1. Перенос операции во внешний слой через эрмитову симметрию
    Поскольку базовые аксиомы линейности (аддитивность и однородность) заданы строго для первого аргумента, переставим аргументы местами. Для этого применим аксиому эрмитовой симметрии:
    $ ⟨x, alpha y + beta z⟩ = overline(⟨alpha y + beta z, x⟩) $

    ==== Шаг 2. Раскрытие сложения по первому аргументу
    Теперь внутри комплексного сопряжения линейная комбинация оказалась на первом месте. Применим аксиому аддитивности по первому аргументу:
    $ overline(⟨alpha y + beta z, x⟩) = overline(⟨alpha y, x⟩ + ⟨beta z, x⟩) $

    ==== Шаг 3. Использование свойств комплексного сопряжения для суммы
    Из теории комплексных чисел известно, что сопряжение суммы равно сумме сопряжений ($overline(a + b) = overline(a) + overline(b)$). Разобьем наше выражение на два независимых слагаемых под общим знаком сопряжения:
    $ overline(⟨alpha y, x⟩ + ⟨beta z, x⟩) = overline(⟨alpha y, x⟩) + overline(⟨beta z, x⟩) $

    ==== Шаг 4. Вынесение скаляров по первому аргументу
    Применим аксиому однородности по первому аргументу внутри каждого комплексного сопряжения, вынося параметры $alpha$ и $beta$ за знак скалярного произведения:
    $ overline(⟨alpha y, x⟩) + overline(⟨beta z, x⟩) = overline(alpha dot ⟨y, x⟩) + overline(beta dot ⟨z, x⟩) $

    ==== Шаг 5. Использование свойств комплексного сопряжения для произведения
    Из свойств комплексных чисел известно, что сопряжение произведения равно произведению сопряжений ($overline(a dot b) = overline(a) dot overline(b)$). Применим это правило к обоим слагаемым:
    $ overline(alpha dot ⟨y, x⟩) + overline(beta dot ⟨z, x⟩) = overline(alpha) dot overline(⟨y, x⟩) + overline(beta) dot overline(⟨z, x⟩) $

    ==== Шаг 6. Возврат к исходному порядку аргументов
    Наконец, применим аксиому эрмитовой симметрии в обратную сторону, чтобы вернуть векторы на свои исходные позиции: $overline(⟨y, x⟩) = ⟨x, y⟩$ и $overline(⟨z, x⟩) = ⟨x, z⟩$.

    Подставляя это, получаем финальное выражение:
    $ overline(alpha) dot ⟨x, y⟩ + overline(beta) dot ⟨x, z⟩ $

    *Итог:* Мы строго вывели цепочку равенств, доказывающую полулинейность:
    $ ⟨x, alpha y + beta z⟩ = overline(alpha)⟨x, y⟩ + overline(beta)⟨x, z⟩ $
    Теорема полностью доказана.

    #bonus[
      Рассмотрим комплексное пространство $bb(C)^2$. Стандартное скалярное произведение векторов $x = (x_1, x_2)$ и $y = (y_1, y_2)$ задается формулой:
      $ ⟨x, y⟩ = x_1 overline(y_1) + x_2 overline(y_2) $

      Возьмем два конкретных вектора с комплексными координатами:
      $ x = (1 + i, 2 - 3i) $
      $ y = (2i, 1 + 4i) $

      - *Шаг 1. Найдем комплексные сопряжения координат вектора $y$* \
        Нам нужно инвертировать знаки перед мнимыми частями координат второго вектора:
        $ overline(y_1) = overline(2i) = -2i $
        $ overline(y_2) = overline(1 + 4i) = 1 - 4i $

      - *Шаг 2. Подставим значения в формулу скалярного произведения* \
        $ ⟨x, y⟩ = (1 + i)(-2i) + (2 - 3i)(1 - 4i) $

      - *Шаг 3. Произведем покомпонентное раскрытие скобок* \
        Вычислим первое слагаемое:
        $ (1 + i)(-2i) = -2i - 2i^2 = -2i - 2(-1) = 2 - 2i $
        Вычислим второе слагаемое (перемножаем методом "каждый на каждого"):
        $ (2 - 3i)(1 - 4i) = 2 dot 1 - 2 dot 4i - 3i dot 1 + 3i dot 4i $
        $ = 2 - 8i - 3i + 12i^2 = 2 - 11i + 12(-1) = 2 - 11i - 12 = -10 - 11i $

      - *Шаг 4. Сложим результаты* \
        $ ⟨x, y⟩ = (2 - 2i) + (-10 - 11i) = (2 - 10) + (-2i - 11i) = -8 - 13i $

      *Ответ:* Скалярное произведение векторов равно комплексному числу $-8 - 13i$.
    ]
  ]
)